let express,listEndpoints,history,httpServer;_7d8‍.x([["default",()=>_7d8‍.o]]);_7d8‍.w("express",[["default",["express"],function(v){express=v}]]);_7d8‍.w("express-list-endpoints",[["default",["listEndpoints"],function(v){listEndpoints=v}]]);_7d8‍.w("connect-history-api-fallback",[["default",["history"],function(v){history=v}]]);_7d8‍.w("http",[["default",["httpServer"],function(v){httpServer=v}]]);




const _7d8‍anonymous=({
  port,
  host,
  srvPath,
  distPath,
  hasTypescript,
  shouldServeApp,
  isInProduction,
}) => {
  return new Promise((resolve, reject) => {
    const app = express();
    const http = httpServer.Server(app);

    if (hasTypescript) {
      require('ts-node/register/transpile-only');
    }

    const server = loadServer(srvPath);

    server(app, http);

    if (isInProduction && shouldServeApp) {
      app.use(history());
      app.use(express.static(distPath));
    }

    http.listen(port, host, err => {
      if (err) {
        reject(err);
      } else {
        resolve(getAppEndpoints(app));
      }
    });
  });
};_7d8‍.d(_7d8‍anonymous);

function getAppEndpoints (app) {
  try {
    return listEndpoints(app);
  } catch (e) {
    return [];
  }
}

function loadServer (file) {
  const moduleIsAvailable = () => {
    try {
      require.resolve(file);
      return true;
    } catch (e) {
      return false;
    }
  };

  const empty = () => {};

  if (!moduleIsAvailable()) {
    return empty;
  }

  const module = require(file);
  return module.default || module || empty;
}
