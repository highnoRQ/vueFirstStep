let serverUrl,logSuccessLunch,server;_7d8‍.x([["default",()=>_7d8‍.o]]);_7d8‍.w("../utils/serverUrl",[["default",["serverUrl"],function(v){serverUrl=v}]]);_7d8‍.w("../utils/logSuccessLaunch",[["default",["logSuccessLunch"],function(v){logSuccessLunch=v}]]);_7d8‍.w("../server",[["default",["server"],function(v){server=v}]]);



const _7d8‍anonymous=({
  defaultOptions,
  srvPath,
  shouldServeApp,
  isInProduction,
  distPath,
  hasTypescript,
}) => args => {
  const run = async (resolve) => {
    const {
      port,
      host,
      localUrl,
      networkUrl,
      localUrlForTerminal,
    } = await serverUrl.findServerUrl(args, defaultOptions);

    const routes = await server({
      port,
      host,
      srvPath,
      distPath,
      hasTypescript,
      shouldServeApp,
      isInProduction,
    });

    if (shouldServeApp && !isInProduction) {
      serverUrl.writeToFile(localUrl);
    }

    logSuccessLunch({
      urls: { local: localUrlForTerminal, network: networkUrl },
      routes,
      isInProduction,
      shouldServeApp,
    });

    resolve();
  };

  return new Promise((resolve) => {
    if (args.delay) {
      setTimeout(() => run(resolve), 300);
    } else {
      run(resolve);
    }
  });
};_7d8‍.d(_7d8‍anonymous);
